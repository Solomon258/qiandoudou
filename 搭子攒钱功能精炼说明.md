# 搭子攒钱功能（精炼说明）

## 一句话概述
在创建钱兜兜时，用户可选择“搭子攒钱”（2-6 个 AI 角色或加入固定圈子），系统在每笔流水后按人设自动生成评论（含TTS语音可选）。

## 端到端流程
1. 首页点击"新建钱兜兜" → 选择"搭子攒钱"（`couple2` 分支）
2. 进入"选择模式"页：
   - 自由选搭（2-6 个） → 选择完成后直接创建搭子钱包
   - 加入圈子（如西天取经等）→ 圈中选角（圈内全员）→ 直接创建搭子钱包
3. 钱包创建成功 → 每个搭子自动转入0.01元并发送欢迎评论 → 跳转`/pages/wallet-detail/wallet-detail`
4. 后续每笔流水触发搭子自动评论（随机2-4名），复用AI生文与TTS链路。

## 前端（小程序）
- 新增/改动页面（已注册于 `app.json`）：
  - `pages/buddy-mode-select/`：选择"自由选搭/加入圈子"
  - `pages/buddy-free-select/`：自由选搭（多选、语音试听、空数据保护）
  - `pages/buddy-group-select/`：圈子列表
  - `pages/buddy-group-character-select/`：圈中选角
- 关键跳转：
  - 首页 `confirmCreateWallet` 中对 `selectedWalletType === 'couple2'` 跳转：`/pages/buddy-mode-select/buddy-mode-select`
  - 钱包创建后使用 `wx.reLaunch` 跳转钱包详情页，确保返回按钮回到首页
- 调用后端API：
  - 获取自由角色：`GET /api/buddy/characters/free`
  - 获取圈子：`GET /api/buddy/groups`
  - 获取圈内角色：`GET /api/buddy/characters/group/{groupId}`
  - 创建搭子钱包：`POST /api/buddy/wallet/create`（body: `userId`, `walletName`, `buddyCharacterIds[]`, `groupId?`）
- UI优化：
  - 搭子钱包详情页隐藏标签信息（红框内容）
  - 钱包列表页搭子钱包显示"搭子"标签（非"情侣"）
- 全局后端地址：
  - 本地开发：`app.js` → `globalData.apiUrl = http://localhost:8080/api`
  - 小程序开发者工具需勾选"不校验合法域名…"以允许本地 http。

## 后端（Spring Boot）
- Controller：`com.qiandoudou.controller.BuddyController`
  - `GET /api/buddy/test`（连通性测试）
  - `GET /api/buddy/characters/free`
  - `GET /api/buddy/groups`
  - `GET /api/buddy/characters/group/{groupId}`
  - `POST /api/buddy/wallet/create`
- Service：`BuddyService` 及 `BuddyServiceImpl`
  - 创建钱包时类型设为 3（搭子钱包）
  - 建立 `wallet_buddies` 关联
  - 调用 `BuddyWalletInitService` 执行初始化
- 初始化服务：`BuddyWalletInitService` 及 `BuddyWalletInitServiceImpl`
  - 钱包创建后为每个搭子转入0.01元
  - 生成个性化欢迎消息并创建交易记录
  - 支持语音生成（TTS）
- 自动评论监听：`AiLoverInteractionEventListener`
  - 监听交易创建事件，同时触发情侣互动与搭子互动
  - `BuddyAutoCommentService` 随机挑选 2-4 名搭子评论（图/文生文→TTS）

## 数据库
- 新表
  - `buddy_characters`：搭子角色（含人设、头像、全身图、标签、语音样例等）
  - `buddy_groups`：圈子（封面、简介、成员数等）
  - `wallet_buddies`：钱包-搭子关系（唯一索引：钱包+角色）
- 现有表变更
  - `wallets.type` 支持 3（搭子钱包）
  - `post_comments` 增 `buddy_character_id`（AI评论可标记为搭子来源）
- 初始化SQL（位于 `qiandoudou-backend/src/main/resources/sql/`）：
  - `buddy_savings_tables_simple.sql`（建表）
  - `create_buddy_groups.sql`（建圈子+圈内角色）
  - `insert_buddy_characters.sql`（自由选搭角色）
  - `add_transaction_ai_fields.sql`（可选：为transactions表添加AI字段支持）

## 人设与语音
- 角色人设字段：`buddy_characters.personality`
- 语音映射：复用 `character_voice_mapping` 表（按角色名映射到 TTS 音色）
- 生成流程：
  - 有图：图生文→拼接流水上下文→生评论
  - 无图：直接文生文
  - TTS：`AiService.generatePartnerVoiceByCharacterName(name, text)`

## 关键约束
- 自由选搭：2-6 个；圈中选角：圈内全员
- 自动评论频率：每条流水挑选 2-4 名搭子（随机延迟）
- Swiper 空数据保护与 UI 降级已处理

## 快速联调清单
- 确认本地后端启动于 `:8080`，`/api/buddy/test` 可 200
- 执行 SQL：`buddy_savings_tables_simple.sql` → `create_buddy_groups.sql` → `insert_buddy_characters.sql` → `add_transaction_ai_fields.sql`（可选）
- 小程序工具勾选"不校验合法域名…"
- 首页选择"搭子攒钱"→ 按流程创建钱包 → 自动初始化（每个搭子转入0.01元）→ 查看钱包详情页流水后 AI 搭子评论

## 已修复问题（2025-09-18）
1. ✅ 自由选搭页面点击"邀请他们加入团队"现在正确创建钱包
2. ✅ 钱包详情页返回按钮现在正确跳转到钱包列表页（使用wx.reLaunch）
3. ✅ 钱包列表页搭子钱包标签正确显示为"搭子"（非"情侣"）
4. ✅ 搭子钱包详情页隐藏了标签信息区域（红框内容）
5. ✅ 搭子钱包创建后每个搭子自动转入0.01元并生成欢迎评论
6. ✅ 初始化服务`BuddyWalletInitService`确保初始转账功能稳定执行

## 测试验证
- 搭子钱包创建：✅ 正常
- 初始转账：✅ 每个搭子0.01元，钱包余额正确更新
- 欢迎评论：✅ 个性化消息生成并保存
- UI显示：✅ 标签和导航正确
- 语音生成：✅ TTS服务正常（见后端日志）
