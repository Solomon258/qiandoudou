# 搭子钱包背景图功能实现说明

## 功能概述
根据用户需求，为搭子钱包创建功能添加了背景图设置：
1. **自由选搭**创建的钱包使用默认背景图：`https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/res/image/dazi/dazizan_beijingtu.jpg`
2. **圈中选角**创建的钱包使用圈子的背景图（如西天取经小队的卡片背景图）

## 实现方案

### 后端修改

#### 1. BuddyController.java
- 修改 `createBuddyWallet` 接口，新增 `backgroundImage` 参数解析
- 将背景图参数传递给 BuddyService

#### 2. BuddyService.java 和 BuddyServiceImpl.java
- 修改 `createBuddyWallet` 方法签名，新增 `backgroundImage` 参数
- 实现背景图逻辑：
  - 如果有 `groupId`（圈中选角）：优先使用圈子的 `background_image`，如果圈子没有背景图则使用默认搭子背景图
  - 如果没有 `groupId`（自由选搭）：使用默认搭子背景图
  - 如果前端明确传递了背景图，则使用传递的背景图

#### 3. WalletServiceImpl.java
- 在 `createWallet` 方法中为搭子钱包（type=3）添加默认背景图兜底逻辑

### 前端修改

#### 1. buddy-free-select.js（自由选搭页面）
- 在创建钱包请求中添加 `backgroundImage` 参数
- 固定使用默认搭子背景图

#### 2. buddy-group-select.js（圈子选择页面）
- 修改跳转URL，传递圈子的 `backgroundImage` 信息到圈中选角页面

#### 3. buddy-group-character-select.js（圈中选角页面）
- 接收圈子背景图参数
- 在创建钱包请求中传递圈子背景图，如果没有则使用默认搭子背景图

### 数据库更新

#### 新增SQL脚本：update_buddy_groups_background.sql
- 为现有圈子数据添加背景图字段
- 西天取经小队使用其卡片封面图作为背景图
- 其他圈子也使用相应的封面图作为背景图

## 背景图URL配置

### 默认搭子背景图
```
https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/res/image/dazi/dazizan_beijingtu.jpg
```

### 圈子背景图（与封面图相同）
- 西天取经小队：`https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/groups/xitian_cover.jpg`
- 水泊梁山：`https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/groups/liangshan_cover.jpg`
- 三国英雄：`https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/groups/sanguo_cover.jpg`
- 盗墓笔记铁三角：`https://qiandoudou.oss-cn-guangzhou.aliyuncs.com/groups/daomu_cover.jpg`

## 测试验证

### 测试步骤
1. 执行SQL脚本 `update_buddy_groups_background.sql` 更新圈子背景图数据
2. 启动后端服务
3. 在小程序中测试：
   - 通过"自由选搭"创建钱包，验证使用默认搭子背景图
   - 通过"圈中选角"创建钱包，验证使用圈子背景图

### 预期结果
- 自由选搭钱包：显示默认搭子背景图
- 西天取经小队钱包：显示西天取经的卡片背景图
- 其他圈子钱包：显示对应圈子的背景图

## 兼容性说明
- 保持向后兼容，如果前端没有传递背景图参数，后端会自动设置合适的默认背景图
- 如果圈子数据中没有背景图，会自动使用默认搭子背景图作为兜底
