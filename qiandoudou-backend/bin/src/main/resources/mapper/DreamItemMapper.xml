<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiandoudou.mapper.DreamItemMapper">

    <!-- 根据分类获取商品列表 -->
    <select id="selectByCategory" resultType="com.qiandoudou.entity.DreamItem">
        SELECT * FROM dream_items 
        WHERE category = #{category} AND is_active = 1 AND deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 搜索商品 -->
    <select id="searchItems" resultType="com.qiandoudou.entity.DreamItem">
        SELECT * FROM dream_items 
        WHERE is_active = 1 AND deleted = 0
        <if test="category != null">
            AND category = #{category}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (display_name LIKE CONCAT('%', #{keyword}, '%') 
                 OR name LIKE CONCAT('%', #{keyword}, '%')
                 OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 获取启用的商品列表 -->
    <select id="selectActiveItems" resultType="com.qiandoudou.entity.DreamItem">
        SELECT * FROM dream_items 
        WHERE is_active = 1 AND deleted = 0
        <if test="category != null">
            AND category = #{category}
        </if>
        ORDER BY sort_order ASC, id ASC
    </select>

</mapper>
