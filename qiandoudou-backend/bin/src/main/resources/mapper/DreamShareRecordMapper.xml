<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiandoudou.mapper.DreamShareRecordMapper">

    <!-- 根据钱包ID获取分享记录 -->
    <select id="selectByWalletId" resultType="com.qiandoudou.entity.DreamShareRecord">
        SELECT * FROM dream_share_records 
        WHERE wallet_id = #{walletId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID获取分享记录 -->
    <select id="selectByUserId" resultType="com.qiandoudou.entity.DreamShareRecord">
        SELECT * FROM dream_share_records 
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 获取用户分享次数统计 -->
    <select id="countUserShares" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM dream_share_records 
        WHERE user_id = #{userId} AND deleted = 0
    </select>

</mapper>
