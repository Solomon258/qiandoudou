<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiandoudou.mapper.ShareImageMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.qiandoudou.entity.ShareImage">
        <id column="id" property="id" />
        <result column="type" property="type" />
        <result column="target_id" property="targetId" />
        <result column="target_name" property="targetName" />
        <result column="image_url" property="imageUrl" />
        <result column="title" property="title" />
        <result column="description" property="description" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, type, target_id, target_name, image_url, title, description, status, create_time, update_time
    </sql>

    <!-- 根据类型和目标ID获取分享图片 -->
    <select id="selectByTypeAndTarget" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM share_images
        WHERE type = #{type} 
          AND target_id = #{targetId}
          AND status = 1
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <!-- 根据类型获取通用分享图片 -->
    <select id="selectByType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM share_images
        WHERE type = #{type} 
          AND target_id IS NULL
          AND status = 1
        ORDER BY create_time DESC
        LIMIT 1
    </select>

</mapper>
