<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiandoudou.mapper.DreamProgressHistoryMapper">

    <!-- 根据钱包ID获取进度历史 -->
    <select id="selectByWalletId" resultType="com.qiandoudou.entity.DreamProgressHistory">
        SELECT * FROM dream_progress_history 
        WHERE wallet_id = #{walletId}
        ORDER BY create_time DESC
    </select>

    <!-- 获取钱包的里程碑历史 -->
    <select id="selectMilestoneHistory" resultType="com.qiandoudou.entity.DreamProgressHistory">
        SELECT * FROM dream_progress_history 
        WHERE wallet_id = #{walletId} AND milestone_reached IS NOT NULL
        ORDER BY create_time DESC
    </select>

</mapper>
