# 钱兜兜（QianDouDou）项目技术文档

## 项目概览

**项目名称**: 钱兜兜 (QianDouDou)
**项目类型**: 微信小程序 + Spring Boot后端服务
**核心定位**: 社交化储蓄应用 - 个人攒钱、情侣共存、梦想储蓄、剧本互动、AI伴侣、搭子社交

---

## 一、功能模块总体结构

### 1. 用户管理系统
**职责**: 用户注册认证、信息维护、会话管理

**主要功能**：
- 用户登录注册（账密、微信、手机号）
- 用户资料管理（昵称、头像、性别、生日）
- JWT Token认证（24小时过期）

**关键代码位置**：
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/AuthController.java`
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/UserServiceImpl.java`
- 后端实体: `qiandoudou-backend/src/main/java/com/qiandoudou/entity/User.java`
- 前端API: `qiandoudou-frontend/utils/api.js` → `authAPI` 对象

**主要API**：
```
POST   /api/auth/login
POST   /api/auth/register
POST   /api/auth/wechat-login
POST   /api/auth/phone-login
GET    /api/auth/current-user
POST   /api/auth/update-profile
```

---

### 2. 钱包系统
**职责**: 钱包创建管理、转入转出、交易记录、社交属性控制

**钱包类型**：
- 类型1: 个人钱包（单人储蓄）
- 类型2: 情侣钱包（两人共享，可关联AI伴侣）
- 类型3-4: 梦想攒钱钱包（购物/旅行）

**主要功能**：
- 钱包的创建、编辑、删除
- 转入/转出操作（支持图片、备注）
- 设置钱包公开/私密状态
- 背景定制（渐变色/自定义图片）
- 交易记录与统计

**关键代码位置**：
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/WalletController.java`
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/WalletServiceImpl.java`
- 后端Mapper: `qiandoudou-backend/src/main/java/com/qiandoudou/mapper/WalletMapper.java`
- 后端实体: `qiandoudou-backend/src/main/java/com/qiandoudou/entity/Wallet.java` 和 `Transaction.java`
- 前端页面: `qiandoudou-frontend/pages/wallet-detail/`、`create-wallet/`、`transfer-in/`、`transfer-out/`
- 前端API: `qiandoudou-frontend/utils/api.js` → `walletAPI` 对象

**主要API**：
```
GET    /api/wallet/list                 # 用户钱包列表（支持onlyPublic参数）
POST   /api/wallet/create
POST   /api/wallet/transfer-in
POST   /api/wallet/transfer-out
GET    /api/wallet/detail
GET    /api/wallet/transactions
PUT    /api/wallet/set-public           # 设置公开/私密
GET    /api/wallet/public               # 获取公开钱包列表
```

---

### 3. 梦想攒钱系统
**职责**: 为梦想目标（购物或旅行）制定储蓄计划

**核心概念**：
- **梦想商品**: 预设的购物目标（手机、包包等）
- **梦想目的地**: 预设的旅行地点（巴黎、东京等）
- **里程碑**: 0% → 20% → 40% → 60% → 80% → 100%，每个阶段可触发奖励

**主要功能**：
- 梦想钱包创建（选择商品/目的地，设定目标金额）
- 梦想进度自动计算和追踪
- AI生成旅行行程（仅旅行梦想）
- 进度分享功能（生成分享图）
- 梦想奖励配置

**关键代码位置**：
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/DreamWalletServiceImpl.java`
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/DreamWalletController.java`
- 后端实体: `Dream*.java` 系列（DreamItem、DreamReward、DreamProgressHistory等）
- 前端页面: `qiandoudou-frontend/pages/dream-*/`（dream-create、dream-detail、dream-rewards、dream-share等）
- 前端API: `qiandoudou-frontend/utils/api.js` → `walletAPI` 中的梦想相关方法

**主要API**：
```
GET    /api/dream-wallet/items          # 梦想商品列表
POST   /api/dream-wallet/create
GET    /api/dream-wallet/detail/{id}
GET    /api/dream-wallet/rewards/{id}
POST   /api/dream-wallet/generate-itinerary  # AI生成行程
POST   /api/dream-wallet/generate-share      # 生成分享图
```

---

### 4. 搭子系统（伙伴功能）
**职责**: 多人组队，为钱包配置虚拟伙伴，实现社交互动

**核心概念**：
- **搭子角色**: 预设的虚拟人物，每个角色有性格、语音、形象
- **搭子圈子**: 按主题分类的角色集合（如"健身搭子"、"读书搭子"）
- **搭子钱包**: 普通钱包可绑定1个或多个搭子，支持搭子自动评论

**主要功能**：
- 搭子钱包创建（自由选择或从圈子选择）
- 搭子角色详情查询
- 搭子自动评论生成（AI）
- 搭子互动记录追踪

**关键代码位置**：
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/BuddyServiceImpl.java`
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/BuddyController.java`
- 后端实体: `BuddyCharacter.java`、`BuddyGroup.java`、`WalletBuddy.java`
- 前端页面: `qiandoudou-frontend/pages/buddy-*/`（buddy-characters、buddy-groups、buddy-group-select等）
- 前端API: `qiandoudou-frontend/utils/api.js` → 搭子相关API

**主要API**：
```
GET    /api/buddy/characters/free       # 自由搭子角色
GET    /api/buddy/groups                # 搭子圈子列表
GET    /api/buddy/characters/group/{id}
POST   /api/buddy/wallet/create         # 创建搭子钱包
GET    /api/buddy/character/{id}
POST   /api/buddy/comment/generate      # 生成搭子评论
```

---

### 5. AI伴侣系统
**职责**: 为情侣钱包提供虚拟伴侣，实现自动化互动

**主要特性**：
- **AI伴侣关联**: 情侣钱包可选择一个AI伴侣
- **自动评论**: 每笔交易自动生成个性化评论
- **语音合成**: 集成火山引擎TTS，为评论生成语音
- **互动追踪**: 保存所有AI伴侣的互动历史

**主要功能**：
- AI伴侣选择与关联
- AI文案自动生成（基于交易内容和伴侣性格）
- 文本转语音（TTS）
- 互动消息记录

**关键代码位置**：
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/AiPartnerServiceImpl.java` 和 `AiServiceImpl.java`
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/AiController.java`
- 后端实体: `AiPartner.java`、`CharacterVoiceMapping.java`
- 前端页面: `qiandoudou-frontend/pages/ai-partner/`、`ai-chat/`
- 前端API: `qiandoudou-frontend/utils/api.js` → AI相关API

**主要API**：
```
POST   /api/ai/generate-text            # AI文案生成
POST   /api/ai/generate-partner-comment # 生成伴侣评论
POST   /api/ai/generate-voice           # 文本转语音
POST   /api/ai/auto-comment             # 自动评论（关联钱包）
```

---

### 6. 社交系统
**职责**: 用户之间的互动：关注、点赞、评论、消息推送

**主要概念**：
- **公开钱包**: 设置 `is_public=1` 的钱包，会出现在社交圈
- **关注**: 用户可关注某个公开钱包，接收其更新
- **点赞/评论**: 用户可对交易记录点赞和评论
- **消息通知**: 互动（关注、点赞、评论）会产生消息通知

**主要功能**：
- 关注/取消关注钱包
- 点赞/取消点赞交易记录
- 评论交易记录（含语音评论）
- 浏览记录统计
- 互动消息推送与未读计数
- 社交统计（关注数、粉丝数、获赞数）

**关键代码位置**：
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/SocialServiceImpl.java`
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/SocialController.java`
- 后端实体: `UserFollow.java`、`PostLike.java`、`PostComment.java`、`Notification.java`
- 前端页面: `qiandoudou-frontend/pages/home/`（社交Tab）、`social/`、`user-social-profile/`
- 前端API: `qiandoudou-frontend/utils/api.js` → `walletAPI` 中的社交方法

**主要API**：
```
POST   /api/social/wallet/follow        # 关注钱包
POST   /api/social/wallet/unfollow
POST   /api/social/transaction/like     # 点赞交易
POST   /api/social/transaction/unlike
POST   /api/social/transaction/comment  # 评论交易
GET    /api/social/transaction/comments # 评论列表
GET    /api/social/user/social-stats    # 用户统计
GET    /api/social/wallet/social-stats  # 钱包统计
POST   /api/social/wallet/view          # 记录浏览
```

---

### 7. 脚本系统（剧本互动）
**职责**: 通过选择故事情节进行交互式攒钱

**核心概念**：
- **脚本**: 一个完整的故事，分为多个章节
- **章节**: 脚本的分段内容，包含选择项
- **选择**: 每个选择项对应一笔扣费，扣费后解锁下一章
- **进度**: 用户在脚本中的当前位置

**主要功能**：
- 脚本列表与分类浏览
- 脚本详情与章节展示
- 开始脚本游戏
- 做出选择并扣费
- 进度追踪与继续游戏
- 脚本图片详情展示

**关键代码位置**：
- 后端服务: `qiandoudou-backend/src/main/java/com/qiandoudou/service/impl/ScriptServiceImpl.java`
- 后端控制器: `qiandoudou-backend/src/main/java/com/qiandoudou/controller/ScriptController.java`
- 后端实体: `Script.java`、`ScriptChapter.java`、`UserScriptProgress.java`
- 前端页面: `qiandoudou-frontend/pages/script-detail/`、`script-image-detail/`
- 前端API: `qiandoudou-frontend/utils/api.js` → `scriptAPI` 对象

**主要API**：
```
GET    /api/scripts                     # 脚本列表
GET    /api/scripts/{id}                # 脚本详情
GET    /api/scripts/{id}/chapters       # 章节列表
GET    /api/scripts/{id}/chapters/{num} # 章节内容
POST   /api/scripts/start               # 开始脚本
POST   /api/scripts/choice              # 做出选择（扣费）
GET    /api/scripts/progress            # 获取用户进度
```

---

## 二、技术栈概览

### 前端
```
微信小程序原生框架
├── JavaScript/CSS/WXML
├── 本地存储: localStorage（token、userInfo）
├── 网络请求: 微信原生 wx.request
└── 关键库:
    ├── axios-like 请求封装
    └── 事件系统（事件总线）
```

### 后端
```
Spring Boot 2.7.18
├── Spring Framework (Web/Security)
├── MyBatis-Plus 3.5.3.1 (ORM + 代码生成)
├── MySQL 8.0 (数据库)
├── JWT (认证)
└── 关键集成:
    ├── 阿里云OSS (文件存储)
    ├── 火山引擎API (AI文本/图文识别)
    ├── TTS服务 (文本转语音)
    └── 微信API (用户认证)
```

---

## 三、数据库表关系总览

```
核心表：
├── users (用户表)
│   ├── 1-n → wallets (一个用户多个钱包)
│   ├── 1-n → transactions (用户的所有交易)
│   └── 1-n → user_follow (用户的关注记录)
│
├── wallets (钱包表)
│   ├── n-1 → users
│   ├── 1-n → transactions (钱包的交易)
│   ├── n-n → buddy_characters (钱包可有多个搭子)
│   ├── 1-1 → ai_partners (可选）
│   └── 梦想字段 (dream_type, dream_target_amount等)
│
├── transactions (交易记录)
│   ├── n-1 → wallets
│   ├── n-1 → users
│   ├── 1-n → post_comments (交易的评论)
│   └── 1-n → post_likes (交易的点赞)
│
├── scripts (脚本表)
│   └── 1-n → script_chapters
│
├── buddy_characters (搭子角色)
│   ├── n-1 → buddy_groups (所属圈子)
│   └── n-n → wallets (通过wallet_buddies)
│
└── user_follow (关注关系)
    ├── 粉丝ID → 钱包ID
    └── 触发 notifications (通知)
```

**关键表**：
- `users`: 用户信息
- `wallets`: 钱包（包含梦想字段）
- `transactions`: 交易（支持AI伴侣评论）
- `post_comments`: 交易评论（支持语音）
- `post_likes`: 点赞记录
- `user_follow`: 关注关系
- `notifications`: 消息通知
- `buddy_characters`: 搭子角色
- `ai_partners`: AI伴侣
- `scripts`: 脚本
- `dream_items`: 梦想商品/目的地

---

## 四、主要代码位置速查

### 前端目录结构
```
qiandoudou-frontend/
├── pages/
│   ├── home/                    # 首页（钱包+社交）
│   ├── login/                   # 登录
│   ├── user-profile/            # 用户个人页
│   ├── user-social-profile/     # 兜圈圈个人主页
│   ├── wallet-detail/           # 钱包详情
│   ├── create-wallet/           # 创建钱包
│   ├── transfer-in/ & transfer-out/  # 转入转出
│   ├── dream-*/                 # 梦想相关页面
│   ├── buddy-*/                 # 搭子相关页面
│   ├── ai-partner/ & ai-chat/   # AI伴侣页面
│   ├── social/                  # 社交圈
│   └── script-*/                # 脚本页面
│
├── utils/
│   └── api.js                   # API请求封装（authAPI、walletAPI等）
│
├── components/
│   └── share-modal/             # 分享模态框
│
├── static/icon/                 # 图标和静态资源
│
├── app.js                        # 小程序入口
├── app.json                      # 小程序配置
└── app.wxss                      # 全局样式
```

### 后端目录结构
```
qiandoudou-backend/src/main/
├── java/com/qiandoudou/
│   ├── controller/               # 控制层（12个Controller）
│   │   ├── AuthController
│   │   ├── WalletController
│   │   ├── DreamWalletController
│   │   ├── BuddyController
│   │   ├── SocialController
│   │   ├── ScriptController
│   │   ├── AiController
│   │   └── ...
│   │
│   ├── service/                  # 服务接口层
│   │   └── 多个XxxService接口
│   │
│   ├── service/impl/             # 服务实现层
│   │   ├── UserServiceImpl
│   │   ├── WalletServiceImpl
│   │   ├── DreamWalletServiceImpl
│   │   ├── BuddyServiceImpl
│   │   ├── SocialServiceImpl
│   │   ├── ScriptServiceImpl
│   │   ├── AiServiceImpl
│   │   ├── AiPartnerServiceImpl
│   │   ├── TtsServiceImpl
│   │   └── ...
│   │
│   ├── mapper/                   # 数据访问层（MyBatis-Plus）
│   │   ├── WalletMapper
│   │   ├── TransactionMapper
│   │   ├── UserFollowMapper
│   │   ├── PostCommentMapper
│   │   ├── PostLikeMapper
│   │   ├── BuddyCharacterMapper
│   │   ├── ScriptMapper
│   │   └── ...
│   │
│   ├── entity/                   # 实体类（23个Entity）
│   │   ├── User
│   │   ├── Wallet
│   │   ├── Transaction
│   │   ├── AiPartner
│   │   ├── BuddyCharacter
│   │   ├── Script
│   │   ├── ScriptChapter
│   │   └── ...
│   │
│   ├── config/                   # 配置类
│   │   ├── SecurityConfig
│   │   ├── JacksonConfig
│   │   ├── OssProperties
│   │   └── ...
│   │
│   ├── common/                   # 通用工具
│   │   └── Result (统一返回格式)
│   │
│   └── QiandoudouApplication.java  # 启动类
│
└── resources/
    ├── application.yml           # 应用配置
    ├── mapper/                   # MyBatis XML映射文件
    └── sql/                      # SQL脚本（初始化数据等）
```

---

## 五、核心业务流程

### 钱包创建流程
```
1. 用户选择钱包类型（个人/情侣/梦想）
2. 输入钱包信息（名称、金额、背景等）
3. POST /api/wallet/create
4. 后端创建Wallet记录（默认is_public=0）
5. 返回钱包详情
6. 前端跳转到钱包详情页
```

### 转入转出流程
```
1. 用户进入钱包详情
2. 点击转入/转出
3. 输入金额、描述、图片
4. POST /api/wallet/transfer-in (或transfer-out)
5. 后端更新钱包余额，创建Transaction
6. 如果是梦想钱包，计算新的梦想进度
7. 如果关联AI伴侣，生成自动评论
8. 返回成功，前端更新余额显示
```

### 发布到社交圈流程
```
1. 用户进入钱包详情
2. 点击"公开到社交圈"
3. PUT /api/wallet/set-public { walletId, isPublic: 1 }
4. 后端更新钱包is_public字段
5. 钱包出现在社交圈
6. 其他用户可以看到并关注
```

### 社交互动流程
```
1. 用户浏览社交圈 (GET /api/wallet/public)
2. 看到公开钱包和交易记录
3. 用户点赞/评论交易
   POST /api/social/transaction/like
   POST /api/social/transaction/comment
4. 系统生成Notification
5. 被点赞/评论者收到消息提醒
6. 被点赞/评论者进入消息中心查看 (GET /api/user/interaction-messages)
```

---

## 六、核心类与接口速查

### 关键Service类
| 类名 | 职责 |
|------|------|
| `UserService` | 用户管理（注册、登录、资料） |
| `WalletService` | 钱包管理（创建、转入转出、公开设置） |
| `DreamWalletService` | 梦想钱包（进度计算、里程碑触发） |
| `BuddyService` | 搭子系统（钱包搭子关系、角色管理） |
| `SocialService` | 社交功能（关注、点赞、评论、通知） |
| `ScriptService` | 脚本游戏（章节管理、进度追踪、选择扣费） |
| `AiService` | AI通用服务（文案生成、图文识别） |
| `AiPartnerService` | AI伴侣管理（伴侣信息、评论生成） |
| `TtsService` | 文本转语音 |
| `OssService` | 文件上传（阿里云OSS） |

### 关键Mapper接口
| 类名 | 职责 |
|------|------|
| `WalletMapper` | 钱包数据访问 |
| `TransactionMapper` | 交易数据访问 |
| `UserFollowMapper` | 关注关系数据访问 |
| `PostCommentMapper` | 评论数据访问 |
| `PostLikeMapper` | 点赞数据访问 |
| `BuddyCharacterMapper` | 搭子角色数据访问 |
| `ScriptMapper` | 脚本数据访问 |

---

## 七、关键特性与实现

### 1. 公开钱包过滤
**需求**: 前端需要获取不同钱包列表
- 钱包列表页: 获取当前用户的**所有**钱包（包括私密）
- 兜圈圈社交页: 获取当前用户的**只有公开**的钱包

**实现方案**:
- API参数: `GET /api/wallet/list?userId=xxx&onlyPublic=true`
- 后端: Controller接收 `onlyPublic` 参数
- Service层: 根据参数进行Java级别的过滤
- 数据库层: Mapper返回所有钱包，不在SQL层过滤

代码位置:
- 后端: `WalletController.java:52`、`WalletService.java:45`
- 前端: `api.js:195` (getUserWallets)、`user-social-profile.js:202` (调用时传true)

### 2. 社交统计数据
**需求**: 钱包需要显示粉丝数、获赞数、浏览数

**实现方案**:
- 数据表: `user_follows`、`post_likes`、`wallet_views`
- SQL: 使用LEFT JOIN子查询统计各指标
- 字段: `fansCount`、`likesCount`、`viewsCount`

代码位置: `WalletMapper.java:21-40` (getUserWalletsWithPartner)

### 3. AI伴侣自动评论
**需求**: 情侣钱包转账时，AI伴侣自动评论

**实现步骤**:
1. 用户转入/转出 → 创建Transaction
2. 检查钱包是否关联AI伴侣 (`aiPartnerId`)
3. 调用AI API生成评论文案
4. 调用TTS API生成语音
5. 保存评论和语音URL到Transaction

代码位置: `TransactionServiceImpl.java`、`AiServiceImpl.java`

### 4. 梦想进度计算
**需求**: 梦想钱包自动计算进度，达到里程碑时触发奖励

**实现方案**:
- 进度公式: `progress = (currentBalance / targetAmount) * 100`
- 里程碑: 0%, 20%, 40%, 60%, 80%, 100%
- 每次转入后重新计算，检测里程碑变化

代码位置: `DreamWalletServiceImpl.java` → `updateDreamProgress()`

### 5. 搭子自动评论
**需求**: 搭子钱包的交易由搭子自动评论

**实现方案**:
- 关系表: `wallet_buddies` (钱包-搭子多对多)
- 交易时获取所有关联搭子
- 逐个生成搭子评论（AI + TTS）
- 评论记录中标记 `isAiComment=1`

代码位置: `BuddyAutoCommentServiceImpl.java`

---

## 八、部署与配置

### 后端配置
```yaml
# application.yml
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://8.148.206.18:3306/qiandoudou
  jpa:
    hibernate:
      ddl-auto: update
```

### 前端配置
```javascript
// utils/api.js
const BASE_URL = 'https://heartllo.cn/api'  // 生产环境
// const BASE_URL = 'http://localhost:8080/api'  // 本地开发
```

### 外部服务
- 微信小程序AppID: `wx8372421078267cd9`
- 阿里云OSS: `qiandoudou.oss-cn-guangzhou.aliyuncs.com`
- 火山引擎API: Model `doubao-seed-1-6-vision-250815`

---

## 九、常见开发任务

### 添加新功能的模板
1. **后端**: 创建Entity → Mapper → Service → Controller
2. **前端**: 创建页面文件夹 → 添加API调用 → UI开发

### 修改钱包数据结构
1. 后端: 修改 `Wallet.java` 实体类
2. 数据库: 运行迁移脚本添加新字段
3. Mapper: 更新SQL查询
4. 前端: 更新相关页面显示逻辑

### 添加新的AI功能
1. 在 `AiServiceImpl.java` 中添加方法
2. 调用火山引擎API
3. 在相应的业务Service中集成
4. 前端调用新API接口

---

## 十、快速参考

### 常用API端点 (生产环境)
```
基础URL: https://heartllo.cn/api

认证:
  POST /auth/login
  POST /auth/wechat-login

钱包:
  GET /wallet/list?userId=xxx&onlyPublic=true
  POST /wallet/transfer-in
  PUT /wallet/set-public

社交:
  POST /social/wallet/follow
  POST /social/transaction/like
  POST /social/transaction/comment

梦想:
  POST /dream-wallet/create
  POST /dream-wallet/generate-share

搭子:
  GET /buddy/characters/free
  POST /buddy/wallet/create

脚本:
  GET /scripts
  POST /scripts/choice
```

### 本地开发启动
```bash
# 后端
cd qiandoudou-backend
mvn spring-boot:run

# 前端（使用微信开发者工具）
打开 qiandoudou-frontend 目录
```

---

## 文档更新日期
2025-11-04

---

**备注**: 本文档为概览级文档，详细的代码实现细节请参考源代码注释。
